import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
      primary: {
           500: '#ff9800',
           contrastText: '#fff',
           light: '#757ce8',
           main: '#e65100',
    
    },
      secondary: {
        main: '#f44336',
      },
    },

  })
  

export default theme